<section class="rr-banner" style="--banner-image: url('{{bannerImage}}')">
    <div class="container">
        <span class="rr-kicker">{{bannerKicker}}</span>
        <h1>{{bannerTitle}}</h1>
        <p class="rr-banner-lead">{{bannerLead}}</p>
    </div>
</section>
